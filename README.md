# meta-tag-webview-injection
Simple demo application that loads an external URL, then injects a meta tag to the loaded html.

#### How to edit URL

Found in ViewController.swift: ` let url = "https://www.google.com" `

